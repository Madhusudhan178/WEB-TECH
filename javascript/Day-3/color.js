var a=(prompt("Enter a color:"))
let c=a.toLowerCase();
console.log(c)

switch(c)
{
     case "red":
          console.log(`${c} is present`);
          break;
     case "pink":
          console.log(`${c} is present`);
          break;
     case "green":
          console.log(`${c} is present`);
          break;
     default:
          console.log(`${c} is  not present`);
     
}